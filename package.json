{
  "name": "BonsaiApp",
  "devDependencies": {
    "browser-sync": "^1.3.0",
    "gulp": "^3.8.5",
    "gulp-angular-templatecache": "^1.8.0",
    "gulp-sass": "^2.3.1"
  },
  "engines": {
    "node": ">=0.10.0"
  },
  "private": true,
  "scripts": {
    "test": "gulp && git status | grep 'working directory clean' >/dev/null || (echo 'Please commit all changes generated by building'; exit 1)"
  },
  "dependencies": {}// Eventually I will move everything from bower.json to run through package.json on an npm only build
}
